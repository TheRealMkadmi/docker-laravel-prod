#!/command/execlineb -P
fdmove -c 2 1
with-contenv
cd /var/www/html

# Log the command
/usr/local/bin/logger.sh HORIZON "Starting Horizon worker"

# Execute the command and pipe all output to logger
pipeline { php artisan horizon }
/usr/local/bin/logger.sh HORIZON